export const patterns = [
  {
    id: 'sliding-window',
    title: 'Sliding Window',
    icon: '🪟',
    description: 'Maintain a window of elements and slide it across the data to find optimal subarrays/substrings.',
    color: 'from-blue-500 to-cyan-500',
    template: `// Sliding Window Template\nfunction slidingWindow(arr, k) {\n  let windowStart = 0;\n  let windowSum = 0;\n  let maxSum = -Infinity;\n\n  for (let windowEnd = 0; windowEnd < arr.length; windowEnd++) {\n    windowSum += arr[windowEnd]; // expand window\n\n    if (windowEnd >= k - 1) {\n      maxSum = Math.max(maxSum, windowSum);\n      windowSum -= arr[windowStart]; // shrink window\n      windowStart++;\n    }\n  }\n  return maxSum;\n}`,
    problems: ['best-time-to-buy-and-sell-stock', 'longest-substring-without-repeating-characters', 'max-consecutive-ones', 'minimum-window-substring', 'sliding-window-maximum', 'permutation-in-string', 'find-all-anagrams-in-a-string', 'longest-repeating-character-replacement', 'fruit-into-baskets', 'subarrays-with-k-different-integers'],
  },
  {
    id: 'two-pointers',
    title: 'Two Pointers',
    icon: '👉👈',
    description: 'Use two pointers moving toward or away from each other to solve problems on sorted arrays.',
    color: 'from-green-500 to-emerald-500',
    template: `// Two Pointers Template\nfunction pairWithTargetSum(arr, target) {\n  let left = 0, right = arr.length - 1;\n\n  while (left < right) {\n    const sum = arr[left] + arr[right];\n    if (sum === target) return [left, right];\n    if (sum < target) left++;\n    else right--;\n  }\n  return [-1, -1];\n}`,
    problems: ['two-sum-ii-input-array-is-sorted', '3sum', '4sum', 'trapping-rain-water', 'container-with-most-water', 'valid-palindrome', 'reverse-string', 'sort-colors', 'next-permutation', 'remove-duplicates-from-sorted-array', 'merge-sorted-array', 'squares-of-a-sorted-array'],
  },
  {
    id: 'fast-slow-pointers',
    title: 'Fast & Slow Pointers',
    icon: '🐇🐢',
    description: 'Two pointers moving at different speeds to detect cycles or find middle elements.',
    color: 'from-orange-500 to-red-500',
    template: `// Fast & Slow Pointers - Cycle Detection\nfunction hasCycle(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;\n}`,
    problems: ['linked-list-cycle', 'linked-list-cycle-ii', 'middle-of-the-linked-list', 'palindrome-linked-list', 'remove-nth-node-from-end-of-list', 'reorder-list', 'find-the-duplicate-number', 'happy-number', 'circular-array-loop'],
  },
  {
    id: 'merge-intervals',
    title: 'Merge Intervals',
    icon: '📐',
    description: 'Deal with overlapping intervals by sorting and merging.',
    color: 'from-purple-500 to-pink-500',
    template: `// Merge Intervals Template\nfunction mergeIntervals(intervals) {\n  intervals.sort((a, b) => a[0] - b[0]);\n  const merged = [intervals[0]];\n\n  for (let i = 1; i < intervals.length; i++) {\n    const last = merged[merged.length - 1];\n    if (intervals[i][0] <= last[1]) {\n      last[1] = Math.max(last[1], intervals[i][1]);\n    } else {\n      merged.push(intervals[i]);\n    }\n  }\n  return merged;\n}`,
    problems: ['merge-intervals', 'insert-interval', 'non-overlapping-intervals', 'meeting-rooms-ii', 'meeting-rooms', 'minimum-number-of-arrows-to-burst-balloons', 'interval-list-intersections', 'employee-free-time'],
  },
  {
    id: 'cyclic-sort',
    title: 'Cyclic Sort',
    icon: '🔄',
    description: 'Place each number at its correct index when dealing with numbers in range [1, n].',
    color: 'from-teal-500 to-green-500',
    template: `// Cyclic Sort Template\nfunction cyclicSort(nums) {\n  let i = 0;\n  while (i < nums.length) {\n    const j = nums[i] - 1;\n    if (nums[i] !== nums[j]) {\n      [nums[i], nums[j]] = [nums[j], nums[i]];\n    } else {\n      i++;\n    }\n  }\n  return nums;\n}`,
    problems: ['missing-number', 'find-all-numbers-disappeared-in-an-array', 'find-the-duplicate-number', 'find-all-duplicates-in-an-array', 'set-mismatch', 'first-missing-positive', 'find-the-corrupt-pair'],
  },
  {
    id: 'in-place-reversal',
    title: 'In-place Reversal of Linked List',
    icon: '🔃',
    description: 'Reverse links between nodes in-place without extra memory.',
    color: 'from-amber-500 to-orange-500',
    template: `// Reverse Linked List Template\nfunction reverse(head) {\n  let prev = null, curr = head;\n  while (curr) {\n    const next = curr.next;\n    curr.next = prev;\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}`,
    problems: ['reverse-linked-list', 'reverse-linked-list-ii', 'reverse-nodes-in-k-group', 'swap-nodes-in-pairs', 'rotate-list', 'reorder-list', 'palindrome-linked-list'],
  },
  {
    id: 'tree-bfs',
    title: 'Tree BFS',
    icon: '🌊',
    description: 'Traverse tree level by level using a queue.',
    color: 'from-sky-500 to-blue-500',
    template: `// Tree BFS Template\nfunction bfs(root) {\n  const result = [];\n  const queue = [root];\n  while (queue.length) {\n    const levelSize = queue.length;\n    const level = [];\n    for (let i = 0; i < levelSize; i++) {\n      const node = queue.shift();\n      level.push(node.val);\n      if (node.left) queue.push(node.left);\n      if (node.right) queue.push(node.right);\n    }\n    result.push(level);\n  }\n  return result;\n}`,
    problems: ['binary-tree-level-order-traversal', 'binary-tree-level-order-traversal-ii', 'binary-tree-zigzag-level-order-traversal', 'average-of-levels-in-binary-tree', 'minimum-depth-of-binary-tree', 'binary-tree-right-side-view', 'populating-next-right-pointers-in-each-node'],
  },
  {
    id: 'tree-dfs',
    title: 'Tree DFS',
    icon: '🏊',
    description: 'Traverse tree depth-first using recursion or a stack.',
    color: 'from-indigo-500 to-purple-500',
    template: `// Tree DFS Template\nfunction dfs(root, target) {\n  if (!root) return false;\n  if (root.val === target) return true;\n  return dfs(root.left, target) || dfs(root.right, target);\n}`,
    problems: ['binary-tree-inorder-traversal', 'binary-tree-preorder-traversal', 'binary-tree-postorder-traversal', 'maximum-depth-of-binary-tree', 'diameter-of-binary-tree', 'balanced-binary-tree', 'lowest-common-ancestor-of-a-binary-tree', 'validate-binary-search-tree', 'path-sum', 'path-sum-ii'],
  },
  {
    id: 'two-heaps',
    title: 'Two Heaps',
    icon: '⚖️',
    description: 'Use a max-heap and min-heap together to find medians or optimize scheduling.',
    color: 'from-rose-500 to-pink-500',
    template: `// Two Heaps - Find Median (simplified)\nclass MedianFinder {\n  constructor() {\n    this.lower = []; // max-heap (simulated)\n    this.upper = []; // min-heap (simulated)\n  }\n  addNum(num) {\n    this.lower.push(num);\n    this.lower.sort((a, b) => b - a);\n    this.upper.push(this.lower.shift());\n    this.upper.sort((a, b) => a - b);\n    if (this.upper.length > this.lower.length + 1)\n      this.lower.push(this.upper.shift());\n  }\n  findMedian() {\n    return this.upper.length > this.lower.length\n      ? this.upper[0]\n      : (this.lower[0] + this.upper[0]) / 2;\n  }\n}`,
    problems: ['find-median-from-data-stream', 'sliding-window-median', 'ipo', 'maximize-capital', 'schedule-tasks-on-minimum-machines'],
  },
  {
    id: 'subsets',
    title: 'Subsets / Backtracking',
    icon: '🎯',
    description: 'Generate all combinations, permutations, or subsets using backtracking.',
    color: 'from-fuchsia-500 to-purple-500',
    template: `// Subsets Template\nfunction subsets(nums) {\n  const result = [[]];\n  for (const num of nums) {\n    const n = result.length;\n    for (let i = 0; i < n; i++) {\n      result.push([...result[i], num]);\n    }\n  }\n  return result;\n}`,
    problems: ['subsets', 'subsets-ii', 'permutations', 'permutations-ii', 'combination-sum', 'combination-sum-ii', 'combination-sum-iii', 'letter-combinations-of-a-phone-number', 'generate-parentheses', 'palindrome-partitioning', 'n-queens', 'sudoku-solver'],
  },
  {
    id: 'modified-binary-search',
    title: 'Modified Binary Search',
    icon: '🔍',
    description: 'Adapt binary search for rotated arrays, infinite arrays, and other variations.',
    color: 'from-yellow-500 to-amber-500',
    template: `// Binary Search Template\nfunction binarySearch(arr, target) {\n  let left = 0, right = arr.length - 1;\n  while (left <= right) {\n    const mid = Math.floor((left + right) / 2);\n    if (arr[mid] === target) return mid;\n    if (arr[mid] < target) left = mid + 1;\n    else right = mid - 1;\n  }\n  return -1;\n}`,
    problems: ['binary-search', 'search-insert-position', 'find-first-and-last-position-of-element-in-sorted-array', 'search-in-rotated-sorted-array', 'search-in-rotated-sorted-array-ii', 'find-minimum-in-rotated-sorted-array', 'single-element-in-a-sorted-array', 'find-peak-element', 'allocate-books', 'median-of-two-sorted-arrays', 'koko-eating-bananas'],
  },
  {
    id: 'top-k-elements',
    title: 'Top K Elements',
    icon: '🏆',
    description: 'Use a heap to efficiently find top/smallest K elements.',
    color: 'from-emerald-500 to-teal-500',
    template: `// Top K Frequent Elements\nfunction topKFrequent(nums, k) {\n  const freq = {};\n  for (const n of nums) freq[n] = (freq[n] || 0) + 1;\n  return Object.entries(freq)\n    .sort((a, b) => b[1] - a[1])\n    .slice(0, k)\n    .map(e => Number(e[0]));\n}`,
    problems: ['kth-largest-element-in-an-array', 'top-k-frequent-elements', 'sort-characters-by-frequency', 'k-closest-points-to-origin', 'top-k-frequent-words', 'reorganize-string', 'task-scheduler', 'maximum-frequency-stack'],
  },
  {
    id: 'k-way-merge',
    title: 'K-way Merge',
    icon: '🔀',
    description: 'Merge K sorted arrays/lists using a min-heap for efficient selection.',
    color: 'from-violet-500 to-indigo-500',
    template: `// K-way Merge (simplified)\nfunction mergeKSorted(lists) {\n  return lists.flat().sort((a, b) => a - b);\n  // In interviews: use a min-heap for O(N log K)\n}`,
    problems: ['merge-k-sorted-lists', 'kth-smallest-element-in-a-sorted-matrix', 'find-k-pairs-with-smallest-sums', 'kth-smallest-number-in-m-sorted-lists'],
  },
  {
    id: 'dynamic-programming',
    title: 'Dynamic Programming',
    icon: '🧩',
    description: '0/1 Knapsack, Unbounded Knapsack, LCS, LIS, and more DP patterns.',
    color: 'from-pink-500 to-rose-500',
    template: `// 0/1 Knapsack Template\nfunction knapsack(weights, values, capacity) {\n  const dp = Array(capacity + 1).fill(0);\n  for (let i = 0; i < weights.length; i++) {\n    for (let w = capacity; w >= weights[i]; w--) {\n      dp[w] = Math.max(dp[w], dp[w - weights[i]] + values[i]);\n    }\n  }\n  return dp[capacity];\n}`,
    problems: ['climbing-stairs', 'coin-change', 'coin-change-ii', 'longest-increasing-subsequence', 'longest-common-subsequence', 'edit-distance', 'partition-equal-subset-sum', 'target-sum', 'maximum-product-subarray', 'word-break', 'decode-ways', 'house-robber'],
  },
];

export default patterns;
